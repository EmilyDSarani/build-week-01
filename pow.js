// Working Pow: const exponent = (num, exp) => {
//const total = Math.pow(num, exp);
//return total;
//}

const exponent = (num, exp) => {

const total = (num ** exp)

return total;

}

module.exports = exponent